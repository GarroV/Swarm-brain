// swarm-recorder-version — источник истины «какая сборка рекордера последняя» для авто-апдейта.
// Рекордер (Updater.swift) дёргает GET и сравнивает с вшитым CFBundleVersion: сервер новее →
// тихо пересобирается из исходников и перезапускается. GitHub тут НЕ участвует (по требованию).
//
// РАСКАТКА: это наш рубильник. Подними LATEST_BUILD здесь ТОЛЬКО после того, как соответствующий
// номер уже лежит в recorder/VERSION в ветке main и собирается. Тогда все рекордеры
// (в т.ч. у маркетинг-команды) тихо обновятся в простое. Плохую сборку не пушим — это сломает всех.
//
// Деплой: supabase functions deploy swarm-recorder-version --no-verify-jwt (публичный GET, без секретов).

// Держать в синхроне с recorder/VERSION (ветка main). Поднимать ПОСЛЕ мёрджа+проверки сборки.
// build 22 (2026-08-19): ЗАЩИТА ОТ ПОТЕРИ ЗАПИСЕЙ (issues #23/#24/#27) + починка релизного
// конвейера (#40). Клиент: decision=defer больше НЕ удаляет аудио — карантин `failed/<id>/`
// (3-суточный потолок), уведомление «транскрибирует @кто», пункт меню «Дослать мою запись» с
// ПЕРЕЗАЯВКОЙ. Claim несёт recorded_seconds → сервер отдаёт транскрибацию более полной записи
// (арбитраж вместо «кто первый нажал стоп»), meeting-claim + миграция recorded_seconds раскатаны
// отдельно и раньше клиента (обратно совместимы).
// ⚠️ Одновременно это ПЕРВЫЙ реально опубликованный asset с build 19: сборки 20 и 21 молча не
// собирались (шелл-ошибка `$BUILD…` в build-app-ci.sh + 22 trailing comma по коду, невалидные на
// раннере), поэтому LATEST_BUILD=21 две недели раздавал ссылку на НЕСУЩЕСТВУЮЩИЙ файл → новые
// установки падали на 404. Подъём до 22 чинит и это.
// Проверено фактически: CI собрал asset (universal arm64+x86_64, minos 13.0, 832 КБ) → скачан и
// прогнан `--selftest-quarantine` 7/7 ok на ИМЕННО ТОМ бинаре, что получат люди; `--selftest`
// (захват→claim→upload→done) против ПРОДА ✅; арбитраж воспроизведён на проде на сценарии
// инцидента 17.08 (196с vs 8785с → перехват, recorded_seconds=8785), тестовые встречи убраны.
// НЕ проверено: GUI-прогон реальной записи со стопом (на машине владельца в это время работал
// его рабочий рекордер — ставить рядом вторую сборку было нельзя).
// build 21 (2026-08-19): watchdog «собеседник не пишется» перестал спамить на обычных паузах
// разговора (владелец: «очень часто, со звуком... есть ли смысл?»). Порог для АКТИВНОГО
// уведомления отвязан от порога тихой само-пересборки (8с — как был, дёшево) и поднят до 50с
// на отдельных, не сбрасываемых пересборками часах (lastGenuineSoundUptime) — в духе исходной
// design-спеки 2026-07-15, которая сама требовала «десятки секунд», а реализация (8с) её
// нарушала. Кулдаун теперь на всю запись (notifiedThisRecording), а не на один эпизод тишины —
// раньше каждая новая пауза ≥16с получала новый алерт. Убран звук уведомления. Текст переписан
// без необоснованной атрибуции Bluetooth (та ветка обрабатывается отдельно и тихо, до этого
// уведомления не доходит). Идея жива (инцидент 2026-07-15, тап завис насмерть, звук собеседника
// молча терялся всю встречу) — правилась реализация, не концепция. Deferred: различение «тап
// реально сдох» vs «оба молчат» вторым сигналом, не завязанным на амплитуду (issue #36).
// Проверено: swift build + build-app.sh + живой --selftest ✅; логику тика (приватные поля,
// без тестового таргета) проверил изолированной численной моделью — 90с тишины подряд → 1
// уведомление на 50с; обычный диалог (паузы 12-20с) → 0 уведомлений (было бы ~4); повторная
// тишина после первого срабатывания в той же записи второго уведомления не даёт. НЕ проверено
// на реальном звонке/Bluetooth-гарнитуре. Тег recorder-build-21.
// build 20 (2026-08-18): ФИКС ГОНКИ АВТО-АПДЕЙТЕРА (issue #33, репорт: «рекордер периодически
// закрывается и не ловит встречи из календаря»). Между проверкой лока «не идёт запись» и
// подтверждением смерти процесса (до ~30с) рекордер ещё жив и мог сам начать запись — лок
// появлялся СНОВА уже после kill. Старая ветка на этот случай выходила БЕЗ open(APP_PATH),
// хотя процесс уже гарантированно мёртв — рекордер оставался закрытым до ручного перезапуска,
// календарные встречи в это окно молча пропадали. Теперь эта ветка всегда перезапускает
// (своп бинарника в этом цикле по-прежнему пропускается, чтобы не подменить .app под
// незавершённой записью). Проверено: swift build чисто, bash-синтаксис self-update.sh валиден,
// гонка поведенчески воспроизведена в изолированном harness (open не звался → фикс: звался),
// живой --selftest на build-app.sh ✅ (claim→upload, system=70942 mic=50358). НЕ проверено на
// реальном цикле авто-апдейта (нужен реальный race на живой машине) — риск принят владельцем.
// Тег recorder-build-20.
// build 19 (2026-08-06): ОБРЕЗКА ТИШИНЫ перед Whisper (SilenceTrimmer): mic-дорожка ~85% тишины →
// −~60% Whisper-минут. Речевые блоки с offset реального старта (порядок реплик sys/mic цел, сервер
// не трогаем), fallback на весь файл при сбое/плотной дорожке, пустой mic не грузится. Проверено на
// проде: реальный бэкап 45 мин вместо 112, порядок реплик цел, тезисы качественные; живой --selftest ✅.
// ВКЛЮЧАЕТ ранее не раскатанные build 17 (календарный конец НЕ рубит активный звонок — стоп только
// по тишине) и build 18 (dismissedKeys с TTL вместо «навсегда»). Тег recorder-build-19.
// build 15 (2026-07-23): ФИКС РЕГРЕССА build 14 — recWatchTick снова тикает. В 14 startCallEndWatch
// вызывался из Task без @MainActor (после await recorder.start()) → на фоновом потоке; Timer.scheduledTimer
// садился на мёртвый фоновый run loop → авто-стоп (тишина/календарь/крышка) и чекпоинт-ротации МОЛЧАЛИ.
// Фикс: Timer(timeInterval:)+RunLoop.main.add внутри DispatchQueue.main.async — таймер всегда на main.
// Проверено ВЖИВУЮ: строки `tick` в /tmp/swarm-calldetect.log каждые 5с на реальной записи. Тег recorder-build-15.
// build 14 (2026-07-23): фикс краша на стопе (deinit-deadlock ProcessTapSystemRecorder — EXC_BREAKPOINT),
// сторож конца звонка армится ДО показа панели (иначе не было авто-стопа/ротаций/меты), meta.json
// пишется синхронно на триггере ротации (recovery видит актуальные сегменты), recovery валидирует
// сегменты через AVAudioFile вместо слепого dropLast + карантин в failed/ вместо удаления аудио.
// Включает build 12 (чекпоинты) и build 13 (авто-стоп на сон/крышку). Тег recorder-build-14.
// Проверено: чистая сборка тега, --selftest (claim→upload→done), heartbeat=14, живой тест quarantine
// (папка с аудио НЕ удаляется), адверсариал-верификация раскатки (3 линзы) → GO, 0 блокеров.
// build 6 (2026-07-15): название встречи сразу в поле панели — реальное (календарь/комната) или
// дефолт «Встреча <имя пользователя macOS> · <дата, время>» вместо пустого плейсхолдера. Тег recorder-build-6.
// build 5 (2026-07-15): надёжность записи собеседника (Фаза 1) — устранён самодедлок AudioDeviceStop
// (разделены очереди IOProc/HAL + таймаут на стоп), watchdog нулей с пересборкой и детектом смены
// формата устройства (BT-профиль) + честный сигнал «собеседник не пишется», сняты триггеры build 4.
// Тег recorder-build-5, build-app.sh + смоук --selftest ✅. Обкатано на реальной встрече (AirPods, 7+ мин).
// build 4 (2026-07-09): heartbeat-мониторинг рекордера (SwarmClient.heartbeat → meeting-heartbeat;
// сервер ловит оборванную запись / истечение токена). Тег recorder-build-4, build-app.sh ✅ (подпись валидна).
// build 3 (2026-06-30): бэкап аудио держится до публикации в базу + потолок 3 суток. Тег recorder-build-3.
const LATEST_BUILD = 22;
// URL предсобранного .app (GitHub Release asset). Установщик и апдейтер качают отсюда — сборки
// из исходников на машине юзера больше нет (issue #19). Держать имя ассета в синхроне с CI/релизом.
const ASSET_URL = `https://github.com/GarroV/Swarm-brain/releases/download/recorder-build-${LATEST_BUILD}/SwarmRecorder-${LATEST_BUILD}.zip`;

Deno.serve((req: Request) => {
  if (req.method === "OPTIONS") {
    return new Response(null, {
      headers: {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Methods": "GET, OPTIONS",
      },
    });
  }
  if (req.method !== "GET") {
    return new Response("Method Not Allowed", { status: 405, headers: { "Allow": "GET, OPTIONS", "Access-Control-Allow-Origin": "*" } });
  }
  return new Response(JSON.stringify({ build: LATEST_BUILD, url: ASSET_URL }), {
    status: 200,
    headers: {
      "Content-Type": "application/json; charset=utf-8",
      "Cache-Control": "public, max-age=300",
      "Access-Control-Allow-Origin": "*",
    },
  });
});
