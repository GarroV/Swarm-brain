// Справка бота — единый текст со ссылками (без inline-кнопок). Веб-приложение называется Swarm.
// Ссылки: веб — HTML <a href>; команды (/recordertoken, /granola, /setup …) Telegram делает
// тапабельными сам. Привязка Google-календаря — в вебе Swarm → Настройки (команды бота нет).

const WEB_URL = "https://swarm-brain.pages.dev";

export function getHelpText(): string {
  return (
    "<b>Swarm — справка</b>\n\n" +

    `🌐 <b>Swarm</b> — <a href="${WEB_URL}">веб-приложение</a>: задачи, встречи, база знаний, поиск. ` +
    "Вход одним кликом через Telegram, ставится как приложение (на компьютере — «Установить» в браузере, на телефоне — «На экран Домой»).\n\n" +

    "<b>📥 База знаний</b>\n" +
    "Печатаешь текст — бот <b>ищет ответ по базе</b> (по умолчанию это вопрос). /ask [вопрос] — то же явно.\n" +
    "<b>Сохранить запись:</b> перешли сообщение, либо пришли 📎 файл (PDF/Excel/TXT/CSV) · 🎤 голос · 🖼 фото · 🔗 ссылку — сохранятся сами (с расшифровкой/тезисами). Текстом — «сохрани: …» / «добавь в базу: …» или /add [текст].\n" +
    "/status — статистика базы\n\n" +

    "<b>🎙 Встречи</b>\n" +
    "Прилетают автоматически (Read.ai · Granola · рекордер) → /meetings — вычитка тезисов.\n\n" +

    "<b>🎙 Рекордер встреч (Mac)</b>\n" +
    "Приложение в меню-баре пишет звук созвона → сервер делает расшифровку и тезисы → встреча в /meetings.\n" +
    "1. /recordertoken → вставь присланную команду <code>curl … | bash</code> в Терминал на Mac → приложение встанет в /Applications с токеном; выдай разрешение на запись в System Settings.\n" +
    `2. Привяжи Google-календарь: <a href="${WEB_URL}">Swarm</a> → Настройки → Google Calendar. Без этого рекордер не видит встреч и не предложит запись сам.\n` +
    "Токен рекордера отдельный (на год), /mytoken его не трогает · отозвать: /revokerecordertoken\n\n" +

    "<b>📓 Granola</b>\n" +
    "Подтягивается раз в час → /meetings. Подключить: /connect granola <code>&lt;API-ключ&gt;</code> (Granola → Settings → API Key) · вручную: /granola · отключить: /disconnect granola\n\n" +

    "<b>✅ Задачи</b>\n" +
    "/tasks — активные · /tasks [имя или страна] — фильтр · /addtask — создать вручную\n\n" +

    "<b>👥 Команда</b>\n" +
    "/users — список и профили · /users add @username · /users remove @username\n\n" +

    "<b>🖥 Claude Desktop (MCP)</b>\n" +
    "/setup — авто-настройка (одна команда в Терминал, macOS) · /mytoken — токен · /connect_claude — вручную · /claude — инструкции с твоим Telegram ID\n\n" +

    "/feedback — фидбек · /reset — сброс · /help — справка"
  );
}
