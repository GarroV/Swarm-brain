# Блок: identity

## Назначение

Даёт `scriba` законную личность на сервере: он приходит со своим токеном, но действует **от имени
человека**. Отдельный блок, потому что это единственное место, где расширяется авторизация, — вся
новая власть собрана здесь и здесь же накрыта блокирующими тестами. Корень графа: без него ни один
серверный вызов бота не проходит.

## API-контракт

```ts
// _shared/agent-auth.ts
export type TokenKind = "recorder" | "recorder_prev" | "mcp" | "bot";
export interface AgentIdentity { telegramId: number; groupId: string | null; kind: TokenKind }

export async function resolveActingIdentity(
  supabase: SupabaseClient, req: Request, onBehalfOf?: number,
): Promise<AgentIdentity>;
```

Подмена личности происходит **один раз, на входе**. После неё `identity.telegramId` — человек, и весь
существующий код (десять мест в `meeting-claim`, проверка владельца в `meeting-ingest`, запись статуса
в `meeting-heartbeat`) продолжает работать без правок.

Хранение: **отдельная таблица служебных агентов** (не `allowed_users` — это таблица людей, служебная
строка в ней молча сделала бы бота сотрудником в выборках команды).

## Зависимости

Продуктовых блоков нет — корень графа. Внешнее: существующая `_shared/agent-auth.ts`, таблица
`allowed_users`, Supabase.

## Definition of Done блока

| этап | пункт |
| --- | --- |
| 1 | миграция таблицы служебных агентов накатана и обратима (только добавление) |
| 1 | `kind` присутствует в `AgentIdentity`; `classifyToken` покрыт тестами на все виды токенов |
| 1 | **блокирующий:** токен `kind: "recorder"` не может передать `on_behalf_of` → 403 |
| 1 | **блокирующий:** бот не может указать человека из чужого воркспейса → отказ |
| 1 | **блокирующий:** токен бота без `on_behalf_of` не даёт прав ни на что |
| 1 | `claim_owner` и `owner_id` после подмены — человек, не бот (проверено тестом) |
| 1 | `bumblebee` прогнан после правок и работает как раньше |
| 1 | secret-review пройден: расширение авторизации осмотрено отдельно |
| 1 | `ARCHITECTURE.md` и `QUICK_REF.md` обновлены тем же коммитом |

## Состояние

**T149 (D017) — в работе, 2026-09-26.** План: миграция `meeting_invites`; `_shared/meeting-invite.ts`
(разбор ссылки, сверка приглашения — чистые функции, тесты вперёд); `swarm-api` `POST /meeting-invites`
(человек); `meeting-invite` (агент без `X-On-Behalf-Of` забирает ожидающие своего воркспейса);
`meeting-claim` ручная ветка агента — только по `invite_id` + `join_url`, гашение условным UPDATE.

**Где стоим (2026-09-26).** T010–T014 закрыты раньше. **T017 (#371, D016) закрыт после возврата с
приёмки**: ревью нашло обход — сверку включал `identity_kind`, выбранный самим агентом. Исправлено:
тип встречи агента сервер определяет по форме ключа, состав берёт сам. Код —
`meeting-claim/agent-scope.ts` (`resolveAgentScope`, `mayJoinExisting`, `keyShape`), проводка в
`meeting-claim/index.ts`. T015 закрыт частично (см. ниже). Миграций не потребовалось.

**Что теперь для агента невозможно.**
- выдать календарное событие за комнатную или ручную встречу (и наоборот) — 400;
- завести календарную встречу на человека, которой нет в его Google-календаре — 403;
- подсунуть свой состав: у календарной встречи он берётся из события Google, у комнатной — пустой;
- встать в чужую открытую встречу: календарную — если её нет в календаре человека (даже когда его
  почта в составе строки), комнатную — если человека нет в составе.
Ручная встреча — без сверки (D016); вопрос о ней вынесен владельцу (Q008), в коде — комментарий.

**Чем проверено.**
- `agent-scope.test.ts` — 21 тест. 12 порч модуля, все красные, возврат копией: тип не сверяется с
  формой; произвольный ключ = комната; любая форма комнаты; любая форма календаря; состав
  календаря из тела; состав комнаты из тела; календарная строка по составу; агент без контекста
  сверки; нет сверки события; календарь упал = пропуск; ручная ветка тронута; люди сверяются.
- Живой смоук `meeting-claim` на своём стенде (`supabase start`, `project_id=scriba-identity`,
  порты 4350–4353, подделан только Google): календарный ключ под `room`/`manual` и комната под
  `calendar` — 400; чужая календарная — 403; своя новая календарная с подделанным составом — 200,
  в строке состав из Google; комната с подделанным составом Боба — отдельная встреча, состав `[]`;
  комната в час календарной встречи Боба, где почта Алисы в составе строки, но встречи нет в её
  календаре, — отдельная встреча; комната в час командного созвона из её календаря — склейка с ним;
  ручная — 200; рекордер человека (ключи любой формы) — 200 как раньше.
- Порчи проводки: снята замена состава → в строку лёг подделанный `x@evil.io`; снят фильтр
  склейки → агент встал во встречу Боба. Снят весь вызов сверки → вместо 400 пришёл 403: второй
  барьер (`mayJoinExisting` без контекста = отказ) удержал встречу Боба. После возврата — всё как выше.
- `./scripts/check` зелёный целиком (сервер 721 тест; пропуски только объявленные гейтом).

**T015 (регресс `bumblebee`).** Живого прогона рекордера нет (macOS-приложение, пишет в прод).
Вместо него: личность рекордера не меняется (`agent-auth-resolve`), сверка людей не касается
(тесты «люди…», смоук рекордером). Остаётся за владельцем: прогон рекордера после раскатки.

**Что открыто.**
- Q008 (владельцу): ручная встреча агента без сверки. Строки ручных встреч агента хранят состав из
  тела; склейка людей (`findMeetingByRoster`) не отличает такие строки от прочих.
- Остаточный риск D016: комнатная встреча без состава открыта агенту любого человека воркспейса.
- Ключ календарной встречи собирается одной функцией `calendarKeyOf` (`_shared/calendar-key.ts`,
  T145): её зовут `meeting-current` и `agent-scope.ts`, регулярка `CALENDAR_KEY` для `keyShape` —
  там же. Тесты: `meeting-current/index.test.ts` сверяет `identity_key` с форматом и с
  `calendarKeyOf`, `agent-scope.test.ts` — что собранный ключ `keyShape` узнаёт как `calendar`.
  Порча формата даты роняет 6 тестов на обеих сторонах, порча регулярки — 10.
  Третья копия сборки осталась в `_shared/meetings-today.ts` (сравнение с ключом идущей записи) —
  вне задачи, решение за диспетчером.
- `zoom:<id>` комнатой агента не считается до T121.
- +1–2 запроса в Google на заявку агента (календарная и комнатная с временем начала).

**Дальше.** Блок на повторную приёмку.

## Статус

in_review · 2026-09-26 — T017 исправлен по ревью; T010–T014 закрыты; T015 частично (нет живого прогона рекордера)
