# Блок: identity

## Назначение

Даёт `scriba` законную личность на сервере: он приходит со своим токеном, но действует **от имени
человека**. Отдельный блок, потому что это единственное место, где расширяется авторизация, — вся
новая власть собрана здесь и здесь же накрыта блокирующими тестами. Корень графа: без него ни один
серверный вызов бота не проходит.

## API-контракт

```ts
// _shared/agent-auth.ts
export type TokenKind = "recorder" | "recorder_prev" | "mcp" | "bot";
export interface AgentIdentity { telegramId: number; groupId: string | null; kind: TokenKind }

export async function resolveActingIdentity(
  supabase: SupabaseClient, req: Request, onBehalfOf?: number,
): Promise<AgentIdentity>;
```

Подмена личности происходит **один раз, на входе**. После неё `identity.telegramId` — человек, и весь
существующий код (десять мест в `meeting-claim`, проверка владельца в `meeting-ingest`, запись статуса
в `meeting-heartbeat`) продолжает работать без правок.

Хранение: **отдельная таблица служебных агентов** (не `allowed_users` — это таблица людей, служебная
строка в ней молча сделала бы бота сотрудником в выборках команды).

## Зависимости

Продуктовых блоков нет — корень графа. Внешнее: существующая `_shared/agent-auth.ts`, таблица
`allowed_users`, Supabase.

## Definition of Done блока

| этап | пункт |
| --- | --- |
| 1 | миграция таблицы служебных агентов накатана и обратима (только добавление) |
| 1 | `kind` присутствует в `AgentIdentity`; `classifyToken` покрыт тестами на все виды токенов |
| 1 | **блокирующий:** токен `kind: "recorder"` не может передать `on_behalf_of` → 403 |
| 1 | **блокирующий:** бот не может указать человека из чужого воркспейса → отказ |
| 1 | **блокирующий:** токен бота без `on_behalf_of` не даёт прав ни на что |
| 1 | `claim_owner` и `owner_id` после подмены — человек, не бот (проверено тестом) |
| 1 | `bumblebee` прогнан после правок и работает как раньше |
| 1 | secret-review пройден: расширение авторизации осмотрено отдельно |
| 1 | `ARCHITECTURE.md` и `QUICK_REF.md` обновлены тем же коммитом |

## Состояние

**Где стоим (2026-09-26).** T010–T014 закрыты раньше. **T017 (#371, D016) закрыт**: агент
ограничен составом встречи — `meeting-claim/agent-scope.ts`, проводка в `meeting-claim/index.ts`.
T015 закрыт частично (живого прогона `bumblebee` не было — см. ниже). Миграций T017 не потребовал.

**Что теперь для агента невозможно.** Завести календарную встречу на человека, у которого её нет в
Google-календаре; встать (склейкой по составу или точным ключом) в уже открытую встречу, где
названного человека нет в составе. Ручные и комнатные встречи — без сверки (D015/D016).

**Чем проверено.**
- `agent-scope.test.ts` — 16 тестов; 9 порч модуля (снята сверка события, «нет календаря =
  пропуск», «календарь упал = пропуск», ключ без даты, присоединение без состава, подтверждённый
  ключ открывает всё, ручная тоже сверяется, формат ключа, люди сверяются) — каждая красная,
  возврат копией.
- Живой смоук обработчика на локальном контуре (`supabase start` с `project_id=scriba-identity`,
  порты 4350–4352, обработчик на 4353, подделан только Google): агент→чужая календарная встреча
  403; календарь не подключён 403; своя командная встреча 200 (та же строка); комната с
  подделанным составом Боба → отдельная своя встреча; ручная 200; конфликт ключа комнаты Боба 403;
  без `X-On-Behalf-Of` 403; рекордер человека 200 как раньше. Три порчи проводки (снята фильтрация
  склейки, снят вызов календарной сверки, снята проверка конфликта ключа) — агент в каждой
  дотягивался до встречи Боба; после возврата копией — снова отказ.
- `./scripts/check` зелёный целиком (сервер 716 тестов; пропуски только объявленные гейтом).
  `ownership.test.ts` поймал `identity.agentId` в логе claim — лог переписан без него.

**T015 (регресс `bumblebee`).** Живого прогона рекордера нет: это macOS-приложение, пишущее в прод.
Доказано вместо него: личность рекордера не меняется (тесты `agent-auth-resolve`), сверка состава
людей не касается (тесты `agent-scope` «люди…», смоук п. «рекордер человека»), heartbeat человека
пишет те же поля. Остаётся за владельцем: прогон рекордера после раскатки.

**Что открыто.**
- Остаточный риск, принятый D016: комнатная встреча без состава открыта агенту любого человека
  воркспейса.
- Ключ календарной встречи собирается в двух местах (`meeting-current/index.ts` и
  `agent-scope.ts` `calendarKeyOf`); `meeting-current` — не файл блока, вынос общей функции —
  диспетчеру. Расхождение даст громкий отказ, а не молчаливый пропуск.
- Сверка ходит в Google: у календарной встречи агента +1 запрос к календарю человека на claim.

**Дальше.** Блок на приёмку.

## Статус

in_review · 2026-09-26 — T010–T014, T017 закрыты и проверены; T015 закрыт частично (нет живого прогона рекордера)
