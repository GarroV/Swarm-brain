# Блок: conference-link

## Назначение

Сервер уже отдаёт ссылку на звонок (`join_url`, появилась ради кнопки «Подключиться», #193). Блок
дополняет её тем, чего не хватает боту: какая это площадка и почему ссылки нет, если её нет.
Маленький блок с большим эффектом — он же закрывает часть запланированной Фазы B room-match.

## API-контракт

```ts
// meeting-current/join-link.ts
export function joinLink(ev: GEvent): string | null;                    // существует
export function conferencePlatform(url: string): "meet" | "kontur" | "zoom" | null;  // новое

// ответ meeting-current
{ join_url: string | null, platform: "meet" | "kontur" | "zoom" | null, reason?: "no_conference_link" }
```

Источники ссылки по порядку: `conferenceData.entryPoints[]` (video, https) → `hangoutLink` →
`location` → **`description`** (новое). Площадка по хосту: `meet.google.com` → `meet`,
`ktalk.ru` / `talk.kontur*` → `kontur`, `*.zoom.us` → `zoom`.

## Зависимости

Продуктовых нет. Внешнее: существующие `meeting-current/index.ts`, `join-link.ts` и его тесты.

## Definition of Done блока

| этап | пункт |
| --- | --- |
| 1 | `description` добавлен четвёртым источником, тест на него есть |
| 1 | `conferencePlatform` покрыта тестами: все три площадки, мусор, неизвестный хост → `null` |
| 1 | ссылки нет → в ответе `reason: "no_conference_link"`, а не молчаливый `null` |
| 1 | существующие тесты `join-link.test.ts` зелёные — поведение `join_url` не изменилось |
| 4 | ключ комнаты `zoom:<id>` заведён на сервере и в рекордере одновременно |

## Статус

in_progress · 2026-09-17 · этап 1 (T020–T022) сделан, идёт сдача

## Журнал работ

| задача | кем | что сделано | чем проверено |
| --- | --- | --- | --- |
| T020 | **myself** — четвёртый источник упёрся в решение, которого нет в контракте: какую из нескольких ссылок описания брать. Решение за мной, исполнителю отдавать нечего | `description` добавлен четвёртым источником в `joinLink`. Из описания берётся ссылка **известной площадки**, и только если такой нет — первая https-ссылка: описание это повестка, документы и справка Google, и первая ссылка там сплошь и рядом не звонок | 4 теста в `join-link.test.ts`; порча (источник выключен) даёт 3 красных |
| T021 | **myself** — задача на 30 строк, запуск исполнителя и чтение его отчёта дороже самой правки | `conferencePlatform(url)`: `meet.google.com` → `meet`, `ktalk.ru`/`*.ktalk.ru`/`talk.kontur.<зона>` → `kontur`, `zoom.us`/`*.zoom.us` → `zoom`. Хост сверяется целиком или по метке домена, не суффиксом строки | 6 тестов: три площадки, мусор, неизвестный хост, подделка хоста, не-https; порча (`endsWith`) даёт красный на подделке |
| T022 | **myself** — меняет контракт между блоками (ответ `meeting-current` для оркестратора), а это исполнителю не отдаётся | `conferenceInfo(ev)` собирает `{ join_url, platform, reason? }`; `index.ts` подставляет её в `meeting`. Ссылки нет → `reason: "no_conference_link"` | 3 теста на `conferenceInfo`; порча (причина убрана) даёт красный |

Исполнителей (`optio`) не запускал ни на одной задаче: ни одна не набрала всех четырёх условий
делегирования — везде решение по контракту оставалось за мной, объём меньше пятидесяти строк
однотипного кода, а T022 меняет контракт между блоками.

### Что решено по ходу и почему

- **`platform` и `reason` лежат ВНУТРИ `meeting`, рядом с `join_url`** — план говорит «дополнительно
  к существующему `join_url`», а он лежит в `meeting`. Верхнеуровневый `reason` в этом эндпоинте уже
  занят другим смыслом («почему встречи нет»: `google_not_connected`, `no_ongoing_event`), и класть
  туда же «почему ссылки нет» значило бы сложить два разных ответа в одно поле.
- **Незнакомая площадка — не отказ.** Ссылка на Teams даёт `platform: null`, но `reason` не ставится:
  ссылка есть, кнопка «Подключиться» работает, просто бот туда не ходит (адаптера нет).
- **`description` добавлен в тип `GEvent`** (`select.ts`, одно необязательное поле). Google отдаёт
  описание сам: `listEvents` не задаёт маску полей — проверено чтением `_shared/google-calendar.ts`.

### Что НЕ сделано (не входит в этап 1)

- Ключ комнаты `zoom:<id>` на сервере и в рекордере (этап 4 в DoD блока) — отдельной задачи в графе
  на него нет, `conferencePlatform` для него подготовлена, но id из ссылки не извлекается.
