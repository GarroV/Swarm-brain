# Блок: orchestrator

## Назначение

Долгоживущая служба: поднимает контейнер на встречу, гасит после, шлёт heartbeat. Отдельно от
контейнера, потому что живёт дольше одной встречи и обязана переживать её падение.

Для MVP достаточно ручного запуска по приглашению из веба (D017); автозапуск по календарю — следующий этап.

## API-контракт

```ts
export async function startForMeeting(joinUrl: string, platform: Platform, onBehalfOf: number, invite?: { id: string; joinUrl: string }): Promise<ContainerId>;
export async function stop(id: ContainerId): Promise<void>;
```

Вход — служба `orchestrator-main.ts`: опрашивает `POST /meeting-invite` (токен агента, без
`X-On-Behalf-Of`) и по каждому приглашению зовёт `startForMeeting(..., invited_by, invite)`;
площадка без адаптера — `join_failed` позвавшему (EN+RU), контейнер не поднимается.

Живёт **внутри WSL2** рядом с Docker, управляет контейнерами через dockerode. Границу Windows/WSL2 не
пересекает.

## Зависимости

`container` (что поднимать), `swarm-client` (чем ходить на сервер), `conference-link` (откуда берётся
ссылка и площадка).

## Definition of Done блока

| этап | пункт |
| --- | --- |
| 1 | контейнер поднимается по ссылке и гасится после встречи |
| 1 | контейнер умер посреди встречи → heartbeat прекратился, существующий watchdog это видит |
| 1 | два контейнера одновременно не мешают друг другу |
| 1 | брошенных контейнеров не остаётся: проверено после падения оркестратора |
| 2 | автозапуск по календарю: бот приходит на встречу сам |

## Состояние

**2026-09-26 · T070 (#352) — ручной запуск работает.** Человек вставил ссылку на созвон в вебе
(серверная часть — identity, D017) → служба оркестратора забирает приглашение → контейнер поднят
от имени позвавшего → бот заявляет встречу по приглашению → заходит → запись сдана. Приглашение на
Контур.Толк/Zoom — контейнер не поднимается, позвавшему `join_failed` с причиной на EN и RU.

Где стою: ветка `feat/invite-trigger`, код и живой прогон готовы. Дальше — сдача блока; веб-поле
(T150) — не моё.

Устройство (`bot/src/orchestrator/`, что нового):

| файл | что держит |
| --- | --- |
| `orchestrator-main.ts` | служба: окружение (отказ по имени переменной), поводок, подхват, цикл приглашений, SIGTERM |
| `invite-trigger.ts` | правила входа: meet → запуск; иначе/сбой запуска → отказ; повтор не поднимает второго бота; сбои опроса и недоставленный отказ — громко в журнал |
| `invite-service.ts` | провода: `InviteClient`, `startForMeeting`, отказ = заявка по приглашению + `join_failed` |
| `claim-request.ts` | тело ручной заявки (`scriba:<runId>`, `invite_id`, `join_url`) — одно для контейнера и отказа |
| `../swarm-client/invites.ts` | клиент `meeting-invite`: без подмены, без повторов, кривое приглашение не роняет пачку |

Остальное устройство (контейнер, поводок, смерть, сироты) — без изменений, см. ARCHITECTURE.md
«Бот scriba: оркестратор встреч».

Проверено:
- `./scripts/check bot` зелёный: 32 файла, 460 тестов, покрытие 100% (база не опустилась).
- Порчи (12, все красные нужным тестом, файл возвращён копией): платформа не проверяется; повтор
  не отсекается; сбой запуска молчит; клиент шлёт X-On-Behalf-Of; кривое приглашение роняет пачку;
  заявка без приглашения; приглашение не едет в контейнер; отказ без заявки; двойник отдаёт
  приглашение дважды / не сверяет комнату / пускает подмену; половина приглашения в окружении.
- Живой смоук (образ `scriba-trigger:dev`, стенд `scriba-trigger`, порты 4410–4411, настоящая
  служба дочерним процессом): `invite` (забрано → метка `scriba.invite` → одна заявка от
  744230399 → запись → стенд пуст), `kontur` (join_failed EN+RU, приглашение погашено, контейнера
  нет), `race` (две службы, три приглашения: каждое — один контейнер и одна заявка, три записи),
  `full` (прежний путь не сломан). Живая порча: контейнер без приглашения в заявке → `invite` красный.
- Двойник `fake-swarm.ts` (чужой, блок swarm-client) расширен только добавлением:
  `/meeting-invite`, сверка приглашения в заявке, режим `requiresInvites`; тесты —
  `fake-swarm-invites.test.ts`; контрактная проверка против сервера — `server-contract.test.ts`.

Открыто, решать не мне:
- **Отказ по Контуру оставляет пустую ручную встречу** и статус приглашения `used`: нотисе нужна
  строка встречи, а своего вида «площадка не поддерживается» у `meeting-notice` нет (блок notices).
  Чище — отказ сразу при вставке в вебе (identity/T150) или отдельный вид нотисы.
- **Потерянный ответ `meeting-invite`** оставляет приглашение `taken` до истечения (15 мин) без
  бота и без отказа — сервер уже пометил его забранным. Лечится на сервере (возврат `taken_at` по
  таймауту), не в клиенте.
- `race` в смоуке доказывает «одна служба не дублирует»; атомарность забора между службами держит
  условный UPDATE сервера — двойник забирает синхронно. В прогоне одна служба взяла все три.
- T147, вторая половина (строка `service_agents` одна на агента) — нужна схема, миграций в волне
  не веду; варианты (а)/(б) — в истории этого файла (`git log -p`).
- #549: `sweepStuckMeetings` метит `failed` встречу бота, идущую дольше 15 мин.
- swarm-client не запускается strip-only Node — контейнер и служба идут с `--experimental-transform-types`.
- CHANGELOG вручную не ведётся (правило проекта: генерируется из git, `scripts/changelog.sh`).
