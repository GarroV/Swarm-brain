<!-- managed by furca: do not change the row format below — fabrica and cursus parse this file -->
# Decisions

<!-- id: Dnnn (D001, D002, ...). who: owner (the owner decided) | auto (decided autonomously by the dispatcher). -->

| id | date | decision | why | who |
|---|---|---|---|---|
<!-- | D001 | 2026-07-29 | Example decision | Example rationale | owner | -->
| D001 | 2026-09-18 | Решения владельца по фиче (пространство = вкладка доски, заморозка кнопкой, принимает любой участник, список + канбан, без нового поля «Ответственный», ссылки над описанием, удалённые — упоминанием, удаление спринта — админ, демо как в артефакте, текущий спринт владельца можно удалить, подготовка через FURCA) записаны дословно в одном месте | Один факт — одно место: канон `docs/decisions/2026-09-18-doska-iniciativ.md`, здесь ссылка, а не копия | owner |
| D002 | 2026-09-18 | Документы FURCA этого проекта пишутся по-русски, хотя `language` в профиле не задан (дефолт en) | Все доки Swarm (ARCHITECTURE, QUICK_REF, спеки, decisions) по-русски, владелец читает по-русски; пользовательские тексты продукта — EN+RU через `dt()`, демо — английское | auto |
| D003 | 2026-09-18 | Компьютер первичен. На телефоне: читаемые «Список», «Сверка», «Все инициативы» с рабочими кнопками сверки и переноса; канбан и набор из пула — только компьютер | Так устроены все доски Swarm (на телефоне доступен только список, `docs/GUIDE.md`); сверку делают на ходу, планирование — за столом | auto |
| D004 | 2026-09-18 | Пока не решён GarroV/furca#140, PR в `main` собирается без файлов состояния FURCA (`tasks.md`, `progress.md`, `docs/furca/`): они живут в ветке `feat/initiatives-board`, в `main` идут код и доки продукта | Ещё два проекта FURCA (`feat/meeting-bot`, `feat/scriba-container`) держат состояние по тем же путям — при слиянии они затрут друг друга. Обратимо: после #140 файлы переедут по новому правилу | auto |
| D005 | 2026-09-18 | Даты спринта считаем строками `YYYY-MM-DD` и `Intl.DateTimeFormat` с `timeZone: 'Europe/Belgrade'`; на Temporal не опираемся | `new Date('YYYY-MM-DD')` разбирается как полночь UTC и в паре с локальным форматированием сдвигает день; Temporal в Safari стабильно недоступен, версия рантайма edge-функций не проверена | auto |
| D006 | 2026-09-18 | В миграции фичи явно прописываем `GRANT` для `service_role` на новые объекты, не полагаясь на автогранты Supabase | С 30.10.2026 Supabase перестаёт выдавать их автоматически во всех существующих проектах; `anon`/`authenticated` доступ не нужен вовсе | auto |
| D007 | 2026-09-18 | Проверку «`tasks.links` — это массив» ставим в базу как `CHECK ... NOT VALID` + отдельный `VALIDATE CONSTRAINT` | Защищает от любого будущего места записи, а не только от кода API; в такой форме не держит тяжёлую блокировку на живой таблице | auto |
| D008 | 2026-09-18 | В списке ссылок задачи: только http/https, не больше 20, повторный одинаковый адрес у одной задачи не добавляется | Схема и правила проверки Plane `IssueLink`, проверенные практикой; лимит — наш, у Plane его нет | auto |
| D009 | 2026-09-18 | Markdown кладётся в буфер синхронно в обработчике кнопки; при отказе показываем текст в поле для выделения руками, ошибку не глотаем | В Safari и Firefox запись в буфер требует жеста пользователя: вызов после похода на сервер молча не сработает | auto |
| D010 | 2026-09-18 | Отменённая задача (`cancelled`) — отдельная цифра вне процента: не входит ни в «сделано», ни в знаменатель (итоги спринта, прогресс инициативы, KPI) | Сейчас отмена задачи повышает процент выполнения, то есть отчёт улучшается от того, что работу не сделали; обратный перекос у Plane (#3542) занижает результат команды | owner |
| D011 | 2026-09-18 | Причину можно вписать и при автоматическом переносе — в окне приёмки, полем без принуждения; `carried_manual` при этом остаётся `false` | Ритуал не удлиняется, но таблица причин перестаёт быть наполовину пустой; обязательная причина заставляет писать «нет времени» ради кнопки | owner |
| D012 | 2026-09-18 | Показываем, сколько раз задача переносилась: «×N» в строке (с двух) и колонка в таблице причин; считаем полем `carry_count` | Ради этого разговора и заводится таблица причин — видно задачу, висящую четвёртый спринт; у Jira такой отметки нет вовсе | owner |
| D013 | 2026-09-18 | Начиная с дня сверки неотмеченные задачи помечаются «не отмечено», число видно в баннере и у каждого человека | Молчание не должно выглядеть как отсутствие проблем; у Linear на этот случай индикатор гаснет | owner |
