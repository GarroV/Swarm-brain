<!-- managed by furca: do not change the row format below — fabrica and cursus parse this file -->
# Decisions

<!-- id: Dnnn (D001, D002, ...). who: owner (the owner decided) | auto (decided autonomously by the dispatcher). -->

| id | date | decision | why | who |
|---|---|---|---|---|
| D001 | 2026-09-17 | Пакет документов FURCA для этой фичи ведётся на русском, а не на дефолтном `en` | Спека 28.08, оба решения владельца и вся dev-документация Swarm — на русском. Пакет на английском разошёлся бы со своим же входом, и владельцу пришлось бы читать два языка об одном предмете. CLAUDE.md проекта прямо разрешает русский для dev-доков | auto |
| D002 | 2026-09-17 | Стройка идёт в отдельной рабочей копии `~/Documents/workbench/worktrees/swarm-meeting-bot-build`, ветка `feat/meeting-bot` | Владелец подтвердил: окно параллельное. Главное дерево занято веткой security-hardening, ещё 6 копий заняты другими задачами — работа в общей папке затирает чужой незакоммиченный WIP | auto |
| D003 | 2026-09-17 | Ветка отведена от `origin/main`, спека принесена слиянием `feat/meeting-bot-spec` | Ветка спеки отставала от main на 175 коммитов — строить на ней значило бы строить на коде трёхнедельной давности. Вливать спеку в main нельзя: пуш в main пересобирает веб на Cloudflare Pages, то есть считается раскаткой | auto |
| D004 | 2026-09-17 | Проверяем бота на встречах, которые создаём сами; свой Google-аккаунт боту заводим позже | Владелец дословно: «Сначала проверить на встречах, которые создаём сами». На своей встрече владелец впускает бота руками — этого хватает, чтобы проверить затею, и не требует заводить аккаунт до того, как бот доказал работоспособность. Следствие: на команду в этом режиме не раскатываем | owner |
| D005 | 2026-09-17 | Звук пишем отдельной дорожкой на каждого участника (подход Vexa), а не общим миксом | Владелец дословно: «враиант с записью отдельного участника нравится больше. у нас по факту виспер все равно дешевый же, и сейчас так и так работают рекордеры у каждого. т.е. с переходом на централизованную запись это будет кажется дешевле». Решение против рекомендации диспетчера (рекомендовался микс как более дешёвый старт). Довод владельца в том, что база сравнения — не «один микс», а сегодняшние N рекордеров на встречу, и относительно них централизованная запись дешевле. Следствие для плана: контракт meeting-ingest расширяется под дорожки участников, §4.3 спеки (таймлайн говорящих из DOM) теряет смысл — имена приходят вместе с дорожками | owner |
| D006 | 2026-09-17 | Пока бот и bumblebee пишут одну встречу, двойную оплату OpenAI терпим | Владелец выбрал вариант «терпим на время проверки». Сравнение бок о бок — это и есть главная проверка качества; отбраковка дубля до транскрибации лишила бы нас сравнения. Перерасход ограничен несколькими встречами, не потоком | owner |
| D007 | 2026-09-17 | Технические развилки 3,4,6,7,8 приняты по рекомендациям без владельца | Токен бота — в отдельной таблице служебных агентов (allowed_users — таблица людей, служебная строка молча сделала бы бота сотрудником в выборках команды); heartbeat бота — в свою запись (иначе watchdog решит, что у человека работает рекордер, и погасит настоящий сигнал); раскрытие — говорящее имя в списке участников; Контур.Толк — браузерный бот, но Talk API проверить до этапа; Zoom — нативный Meeting SDK; оркестратор — внутри WSL2 рядом с Docker | auto |
| D008 | 2026-09-17 | **Отменяет D005.** Пишем общую дорожку и отмечаем по времени, кто говорит, — как и задумывала спека §4.3. Дорожки на участника не делаем | Владелец дословно: «разве мы не можем писать общую дорожку, и отмечать по времени кто из спикеров разговаривает? это ж явно дешевле будет, и у нас что-то такое уже есть». Проверено по коду — да, есть, и даже больше: `Segment { start, end, text, speaker? }` (meeting-processor.ts:64) уже хранит говорящего по интервалу времени, транскрипт так и лежит в базе. Имя вычисляется ровно в ОДНОЙ строке (:395, `track==="sys"?"собеседник":"я"`) — значит подстановка настоящих имён меняет одну строку сборки плюс приём таймлайна, а `Part.track` и восемь мест вокруг него не трогаются вовсе. Сверх того: в BACKLOG.md:315 этот путь уже проработан как «вариант А — active-speaker из UI звонка», и там сказано, что он надёжен только через браузерное расширение — а бот и есть браузер, то есть он снимает главное возражение против варианта А | owner |
| D009 | 2026-09-17 | Бот называется `scriba` — латиницей, со строчной буквы, как `bumblebee` и `granola` | Выбор владельца из пяти кандидатов. Латинское «писец» — тот, кто записывает; два слога, читается с листа. Отклонены: `forager` (пчела-сборщица, точная вселенная, но английское), `notarius` (точнее по смыслу и свободнее, но четыре слога и отдаёт нотариусом), `mellifera` (красиво и из вселенной, но ничего не говорит про запись), `drone` (трутень + «прилетает на встречу», отсеян до показа: в тех-контексте читается как Drone CI и беспилотники). Занятость проверена: домены всех кандидатов заняты, но боту домен не нужен — публичного адреса у него нет по конструкции; значимый риск только у `scriba` — рядом живёт scriban (шаблонизатор .NET, 3970★), другая ниша, при поиске может путаться. Правило написания наследуется от решения 28.08 о `bumblebee`: имя — пользовательская поверхность, в коде остаётся нейтральное | owner |
| D010 | 2026-09-17 | Запись в `CHANGELOG.md` вручную не делаем, вопреки шагу процесса | Правило проекта сильнее: changelog Swarm генерируется из git-истории (`scripts/changelog.sh`), а `docs/CHANGELOG.md` — исторический архив, который руками не трогают. Источник истины — conventional commits, и в этой ветке они соблюдены | auto |
| D011 | 2026-09-17 | Экранный эталон (`docs/furca/design/`) не делаем | У `scriba` нет интерфейса: это служба. Его человеческие поверхности — уведомление в Telegram и очередь вычитки — принадлежат Swarm и уже существуют. Рисовать эталон нечему | auto |
| D012 | 2026-09-17 | Доступ к прод-базе восстанавливает диспетчер сам, вопросом владельцу это не считается | Владелец на вопрос о потерянном доступе ответил поручением разобраться, а не ответом. Пока Q002 стоял `open`, стройка выглядела ждущей владельца, хотя ждать было нечего: это обычная задача T001 | owner |
| D013 | 2026-09-17 | Формат и линт серверного кода проверяются только на файлах, которые трогает стройка; легаси остаётся как есть | Замер 17.09.2026: в существующем коде Swarm 172 файла из 188 не по формату и 133 замечания линта. Переформатировать репозиторий целиком — это огромный диф поверх параллельных веток и нечитаемые дифы блоков, то есть уничтожение той самой пользы, ради которой роль «формат» заведена. Проверка типов при этом идёт по ВСЕМ функциям: правка в `_shared/` ломает потребителей, которых её автор не открывал | auto |
| D014 | 2026-09-17 | Живость MCP-доступа проверяется вызовом, а не статусом сервера и не заметкой в памяти; своей сессии, держащей старый снимок серверов, не верить как признаку поломки | Утренний диагноз «коннектор не авторизован» был верен в момент замера и устарел за часы: владелец починил его в тот же день. Диспетчер продолжал опираться на запись в памяти и повторно просил владельца о том, что уже сделано. Единственный настоящий признак поломки — ошибка в ответе на вызов, и то лишь после проверки, видит ли эта сессия нужный сервер вообще | auto |
| D015 | 2026-09-23 | Бот приходит на встречу по календарю, подключённому человеком (вариант Б); собственного Google-аккаунта у `scriba` не будет. Ручное добавление бота остаётся рабочим путём всегда, а не только до автоматики | Решение владельца, дословно: «006 - вариант б кажется адекватный. хотя конечно надо как-т опредуагать возможные проблемы, чтобы руками бота тоже добавлять, если автоматика не сработает». Свой ящик боту нужно заводить в корпоративном домене и держать в приглашениях, а календарь человека уже подключён в Swarm и даёт ту же встречу без новых сущностей. Автоматика по календарю ломается тихо (токен протух, встречу создали вне подключённого календаря, ссылка сменилась), поэтому ручной запуск — не временная затычка до автоматики, а постоянный запасной путь; о несработавшей автоматике человек должен узнать, а не обнаружить пустую запись | owner |
| D016 | 2026-09-23 | Полномочия токена агента сужаются до состава встречи там, где состав известен: для календарных встреч сервер сверяет названного человека с участниками, ручные запуски остаются без сверки состава | Владелец делегировал выбор дословно: «007 - сделай как лучше». Взят второй вариант в рекомендованной границе: календарная встреча несёт состав, и сверка стоит одного сравнения; у ручной встречи состава нет по определению, и требовать его — значит запретить ручной путь, который решением D015 объявлен постоянным. Без сверки скомпрометированный или ошибшийся токен кладёт приватную запись человеку, которого на звонке не было, а принципы проекта говорят «приватность не расширяется ни на шаг» | auto |
| D017 | 2026-09-26 | Ручной запуск бота — как в Read.ai: в вебе Swarm поле «вставь ссылку на созвон», человек вставляет — бот стучится. Вставка залогиненным человеком И ЕСТЬ приглашение: сервер заводит запись «кто позвал, на какую ссылку, когда» (одноразовая, с коротким сроком), оркестратор берёт её в работу, бот предъявляет её в meeting-claim. Ручную встречу агент заводит ТОЛЬКО по такой записи; без неё ручная ветка закрыта. Основной путь — бот сам приходит по календарю (T100). | Владелец, дословно: «я не понимаю что тебе ответить / обычно бот сам просится в созвон / в ридаи была кнопка где вставляешь ссылку на созвон и он стучится / продумай сам оптимальню логику». Ревью identity показало: ручная ветка без проверки обходит всю календарную сверку D016 — украденный токен бота заводит человеку приватную запись встречи, где его не было. Приглашение от залогиненного человека закрывает это и совпадает с привычным владельцу поведением Read.ai. Заменяет часть D016 про ручные встречи. | auto |
| D018 | 2026-09-26 | Heartbeat бота различается по встрече: поля `agent_last_seen_at` и `agent_last_recording` в `meetings` (вариант «а» из отчёта T147), heartbeat бота несёт `meeting_id`; сторож проверяет молчание по каждой встрече отдельно. Отдельная таблица запусков не заводится. | Строка `service_agents` одна на агента: при двух встречах живой контейнер прячет замолчавший. Бот на встречу один, адресат (`claim_owner`) уже в строке встречи — поля в `meetings` проще таблицы запусков и снимают неоднозначность ручных ключей. Таблица понадобится, только если на встречу придут два агента — такого сценария в спеке нет. | auto |
| D019 | 2026-09-26 | Когда арбитраж `meeting-claim` передаёт право транскрибации от бота рекордеру человека, то же обновление сбрасывает `meetings.agent_last_recording=false` (T153) | Иначе удары бота получают 403, флаг остаётся, и новому `claim_owner` приходит ложный алерт «scriba перестал отвечать» (развилка из сдачи T151). Логику бота владелец делегировал: «продумай сам оптимальню логику» | auto |
