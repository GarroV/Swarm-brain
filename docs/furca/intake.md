<!-- intake.md — анкета: единственный документ, который заполняется вместе с владельцем. Из неё вырастают спека, техплан и блоки, поэтому её полнота определяет качество всего дальнейшего. Заполняется в фазе 1, результат — docs/furca/intake.md в репозитории проекта. -->

# Анкета проекта

<!-- Имя проекта, дата начала. -->

Проект: **доска инициатив в «Спринтах» Swarm Brain** (рабочий слаг `initiatives`), начало подготовки
в FURCA — 2026-09-18. Случай Б: **продолжаем существующий проект** (владелец: «может фурку надо
подключить» → «Да, через FURCA»).

Входные документы прочитаны целиком до анкеты; всё, что в них есть, помечено «из документа»:
- спека — `docs/superpowers/specs/2026-09-18-initiatives-board-design.md` (согласована владельцем);
- решения владельца — `docs/decisions/2026-09-18-doska-iniciativ.md` (пять заходов разговора);
- эталон поведения — артефакт из Claude, `~/Documents/workbench/private/swarm/initiatives-board-artifact.html`
  (вне git: внутренние задачи и имена коллег, репозиторий публичный).

## Как заполнять

- **Сначала секция 0.** Пока не ясно, что у проекта уже есть, остальные вопросы
  задаются наугад: продукту с живыми пользователями нельзя задавать те же
  вопросы, что проекту с нуля. Ответы секции 0 включают и выключают вопросы
  дальше — это правила ветвления, а не справка.
- **Если есть готовый текст — сначала прочитать его, потом спрашивать.** ТЗ,
  спека, переписка, расшифровка голосового: всё, что в нём уже сказано,
  переносится в анкету с пометкой «из документа» и **повторно не спрашивается**.
  Владельцу задаются только пробелы. Переспрашивать то, что человек уже написал,
  — самый быстрый способ получить «да я же всё прислал».
- **«Не знаю» и «решай сам» — полноценные ответы, а не пропуск.** «Не знаю»
  уходит в автоматическое исследование; «решай сам» — в разумный дефолт, и
  дефолт записывается в `decisions.md` как принятый без владельца. Молча пустым
  не остаётся ничего.
- **Метка у каждого вопроса.** `[owner]` — ответ может дать только владелец
  (цели, приоритеты, деньги, доступы). `[research]` — если владелец не знает,
  вопрос уходит в исследование, а не блокирует анкету.
- **Секция, не подходящая проекту, помечается «пропущена: <причина>»** и не
  удаляется: видно, что вопрос рассматривали, а не забыли. Полнота покрытия
  важнее длины. Пропуск — только про **неприменимость** (у продукта нет такой
  поверхности, он не берёт денег): «владелец не знает ответов» пропуском не
  является, «не знаю» — это ответ, и секция остаётся заполненной частично.
- **Уточнять — не больше пяти вопросов на секцию,** и только те, ответ на
  которые меняет архитектуру, набор задач, тесты или запуск. Если ответ ничего
  не меняет — вопрос не задаётся.
- **К каждому вопросу, где это возможно, предлагается рекомендация,** чтобы
  «решай сам» был исполним сразу, а не откладывался.
- **Ответы записываются в файл по завершении каждой секции,** а не в конце
  разговора: анкета — такой же файл состояния, как остальные, и прерванная
  сессия продолжает с последней записанной секции.
- **Значения секретов сюда не пишутся.** В анкете — только имя переменной и где
  лежит значение; сами значения — сразу в `.env` проекта, который не попадает в
  git.

## 0. Стартовая точка

<!-- Применимость: всегда, заполняется первой. Отмечается всё, что уже существует к моменту разговора, — от «есть только идея» до «есть работающий продукт с пользователями». -->

- `[owner]` **Что уже есть?** Отметить всё подходящее и указать, где это лежит.

| Что | Есть? | Где / что именно |
|---|---|---|
| Только идея, больше ничего | нет | — |
| Написанный текст: ТЗ, спека, PRD, переписка, расшифровка голосового | да | спека и решения владельца (см. шапку) — из документа |
| Работающий продукт или его часть | да | Swarm Brain: веб `swarm-brain.pages.dev` (Next.js, Cloudflare Pages), Edge Functions Supabase (`swarm-api`, `swarm-bot`, `swarm-mcp` и др.), бот, MCP, рекордер. Спринты #267 в проде с 09.09.2026 — из документа |
| Код без работающего продукта: наработки, прототип, скрипты | нет | артефакт — эталон поведения, а не код для переноса (спека §4) |
| Данные: дамп базы, таблицы, файлы, выгрузки | да | данные артефакта: 5 направлений, 16 инициатив, 208 задач — **заливка вне этой стройки** (этап 5, отдельный дизайн) — из документа |
| Дизайн: макеты, бренд-гайд, палитра, шрифты | да | дизайн-система веба Swarm (`miniapp/src/components/ui/`, светлая и тёмная темы) + раскладка экранов артефакта |
| Живые пользователи | да | команда воркспейса работает в продукте каждый день (в работе с 23.07.2026, CLAUDE.md) |
| Аналог, который нужно повторить | да | артефакт «Доска инициатив и спринтов»; условие владельца — «точно реализует то что я тебе передал» — из документа |
| Готовый контракт: чужой API, схема обмена, формат файлов | да | собственные контракты Swarm: таблицы `tasks`/`projects`/`sprints`/`sprint_cycles`/`sprint_items`, роуты `swarm-api`, MCP-инструменты — меняются только аддитивно (спека §9) |
| Срок или событие, к которому нужно успеть | не знаю | спрашивается в секции F |

- `[owner]` **Если продукт или код уже есть — что с ним делать:** продолжаем в
  этом же коде, переписываем с нуля, или добавляем отдельную часть рядом?
  <!-- зачем: без ответа агенты будут одновременно читать старое и писать новое, а это худший из вариантов. Рекомендация: продолжать в существующем коде, если он запускается и в нём есть тесты; переписывать — только если он не запускается -->
  → **Продолжаем в этом же коде** поверх спринтов #267: «механику спринта не пишем заново» (спека §1) — из документа.
- `[owner]` **Чей это продукт:** твой личный, внутренний инструмент для команды
  или продукт для клиента? <!-- зачем: определяет, кто отвечает на вопросы по ходу стройки и кому сдаём результат -->
  → Внутренний инструмент команды; вопросы и приёмка — владелец (README, CLAUDE.md).
- `[owner]` **Кто ещё будет трогать код?** <!-- зачем: если не только ты, работа идёт через ветки и разборы изменений, а не прямыми коммитами в основную ветку -->
  → Владелец и параллельные сессии Claude Code (на 18.09.2026 у репозитория 12 worktree). Работа только
  через ветку и PR; в `main` — ночным окном по «да» владельца (CLAUDE.md п.5).
- `[research]` **Работает ли кто-то в этой папке прямо сейчас?** Не про людей вообще, а про эту минуту: вторая сессия, открытый редактор, чужие незакоммиченные правки. Проверяется самому — `git status` (чужое в дереве), `git worktree list`, свежесть последних коммитов. <!-- зачем: две работы в одном каталоге топчут друг друга через общий индекс git. На живом прогоне незакоммиченные файлы соседней сессии уехали в чужой коммит, а запись в общем журнале была затёрта; разбор занял несколько шагов и потребовал владельца. Нашлась чужая работа — стройка идёт в своей копии репозитория (`git worktree`), а не в общей папке -->
  → Проверено 18.09.2026: стройка идёт в своём worktree `~/Documents/workbench/worktrees/swarm-initiatives`
  (ветка `feat/initiatives-board`), чужих правок в нём нет. В основной папке — ветка
  `feat/security-storage-fix` другой сессии и неотслеживаемый файл артефакта владельца. **В ветках
  `feat/meeting-bot` и `feat/scriba-container` идут другие проекты FURCA по тем же путям состояния**
  (`tasks.md`, `progress.md`, `docs/furca/`) — при слиянии в `main` столкнутся; в системе это
  GarroV/furca#140.

### Что меняется дальше в зависимости от ответов

<!-- Это правила ветвления анкеты, а не пояснение. Каждая строка — обязательство: если слева «да», справа выполняется. -->

| Если есть | То |
|---|---|
| **текст ТЗ** | система читает его сама и не переспрашивает то, что там есть; закрытые им пункты помечаются «из документа», спрашиваются только пробелы |
| **работающий продукт** | в секции C появляется обязательный вопрос «что нельзя ломать»; спека обязана отделять существующее поведение от нового; изменения схемы данных — только с планом откáта |
| **живые пользователи** | демонстрационный режим строится на изолированных данных, никогда на настоящих; ни один сценарий проверки не выполняется на живой базе |
| **код без продукта** | ответ «продолжаем / переписываем» становится решением, записанным в `decisions.md`, — иначе он будет пересматриваться в каждом блоке |
| **данные** | заполняется E8: где источник, объём, формат, можно ли трогать оригинал |
| **дизайн** | секция D не спрашивает про визуальное направление — следуем макетам; вопросы про палитру и шрифты снимаются |
| **аналог для повторения** | в секции D уточняется: повторить поведение как есть или взять только идею |
| **готовый контракт** | контракт — это вход, а не предмет обсуждения: в секции C он фиксируется как ограничение, менять его нельзя |
| **срок** | в секции F подрезается глубина первой версии под срок, а не срок под желания |
| **только идея** | анкета проходится целиком, ничего не снимается |

Применено к этому проекту: текст ТЗ — пробелы только срок, подтверждение «что нельзя ломать» и
критерия MVP; работающий продукт — C.«что нельзя ломать» обязателен, схема только аддитивно;
**живые пользователи — ни одна проверка стройки не идёт на прод-базе, только локальный контур
(`supabase start`), демо — изолированный воркспейс `demo`**; данные — E8, оригинал только читаем;
дизайн — следуем дизайн-системе Swarm и раскладке артефакта; аналог — повторяем поведение как есть,
кроме явно отменённого владельцем (спека §4); контракт — только аддитивные изменения.

## A. Продукт

<!-- Применимость: всегда. Без этой секции нет ни спеки, ни критерия «достроили». -->

- `[owner]` **Проблема.** Что болит, у кого, и как эти люди обходятся без
  продукта сейчас? <!-- зачем: без цены текущего положения нечем отличить нужную фичу от красивой -->
  → Большой кросс-командный проект («Качество в Европе») ведётся в отдельном артефакте Claude:
  208 задач живут мимо Swarm, их ведут дважды или не ведут вовсе; у артефакта нет настоящих
  пользователей и прав («как вас подписать»), данные лежат в хранилище артефакта. В Swarm есть
  спринты (#267), но нет пространств, инициатив, сверки и переноса с причиной — из документа.
- `[owner]` **Аудитория.** Кто будет этим пользоваться — одна роль или несколько
  разных? <!-- зачем: роли определяют, нужны ли аккаунты и права; это архитектурная развилка, а не описание -->
  → Участники воркспейса, одна роль; админ отличается только правом удалить спринт (спека §6.2) —
  из документа.
- `[owner]` **Главные сценарии.** Что человек делает в продукте от начала до
  конца — один-три сценария прозой. <!-- зачем: из них получается сквозной смоук, которым проверяется готовность MVP -->
  → Из документа (спека §3):
  1. Ведущий открывает «Спринты», выбирает пространство, набирает черновик спринта (из списка задач,
     из «Все инициативы» или новой задачей в инициативе) и жмёт «Начать спринт».
  2. В день сверки каждый отмечает свои задачи «по плану / риск / проблема» с комментарием; то, что не
     успевает, помечает «к переносу» с причиной.
  3. В конце — «Принять спринт»: фиксируются снимок и итоги, сразу создаётся следующий черновик с
     хвостами; команда добирает задачи и стартует. Аналитика, журнал и выгрузка показывают то же,
     что артефакт.
- `[owner]` **Критерий успеха MVP.** По чему будет видно, что первая версия
  годится? <!-- зачем: это будущий DoD MVP; без него стройка не знает, где финиш -->
  → Черновик из документа, **ждёт подтверждения владельца**: владелец «смотрит и щупает» на проде
  пространство с направлениями и инициативами; спринт проходит полный круг (набор → старт → сверка →
  перенос → приёмка → следующий черновик); семь таблиц аналитики, журнал и выгрузка совпадают с
  артефактом по составу; всё из списка «что нельзя ломать» (секция C) работает как раньше.
- `[owner]` **Что точно НЕ делаем в первой версии.** <!-- зачем: явные границы не дают агентам расширять скоуп по своей инициативе -->
  → Из документа (спека §12): заморозка по времени и код разморозки; переименование `sprints` →
  `board_tabs`; переименование спринта из UI (#297); графики сгорания и оценки в часах; **заливка
  данных «Качества» и склейка с уже существующими задачами** — это этап 5, отдельный дизайн
  («очень аккуратно», после интерфейса).

## B. Поверхности

<!-- Применимость: всегда. Нужна хотя бы одна поверхность, но она может быть и без интерфейса. -->

- `[owner]` **Через что пользуются продуктом?** Веб-страница, Telegram-бот,
  HTTP API, команда в терминале, мобильное приложение — или у продукта вообще
  нет интерфейса: библиотека, служба по расписанию, интеграция между двумя
  системами. <!-- зачем: набор поверхностей задаёт блоки стройки напрямую. Продукт без интерфейса — нормальный случай, а не ошибка заполнения -->
  → Веб (вкладка «Спринты» в `miniapp`) + HTTP API `swarm-api` (новые роуты — модулями). Бот и MCP не
  меняются, кроме подписи поля «ссылки» в истории задачи — из документа.
- `[owner]` **Встраивается ли продукт в существующую систему** — или живёт сам
  по себе? <!-- зачем: встраивание означает чужие контракты и чужой стек, которые нельзя выбирать заново -->
  → Встраивается в Swarm Brain: его стек и контракты — из документа.
- `[owner]` **Язык интерфейса продукта.** <!-- зачем: влияет на тексты и вёрстку. Отдельное правило: демонстрационный режим всегда на английском, независимо от языка продукта -->
  → Двуязычно, EN приоритетный + RU, через `dt(ru, en)` (CLAUDE.md п.7, решение 2026-07-29) — из
  документа. Демо — на английском.
- `[owner]` **Если есть веб-поверхность:** что важнее — телефон или компьютер?
  <!-- зачем: определяет, с какого разрешения начинается вёрстка, а не остаётся «адаптируем потом» -->
  → Не спрашивалось, решено без владельца (D-auto в `decisions.md`): **компьютер первичен**, как у всех
  досок Swarm (на телефоне у них доступен только список, `GUIDE.md`). На телефоне — читаемые виды
  «Список», «Сверка», «Все инициативы» с рабочими кнопками сверки и переноса; канбан и пул — только
  компьютер.
- `[owner]` **Есть ли у продукта публичная и закрытая части** — или всё
  доступно всем? <!-- зачем: наличие закрытой части тянет за собой вход, сессии и проверку прав -->
  → Всё за входом (Telegram-логин / web-JWT); демо — по секретной ссылке в изолированном воркспейсе
  `demo` — из документа (ARCHITECTURE.md).

## C. Функционал

<!-- Применимость: всегда. -->

- `[owner]` **Список возможностей с приоритетом:** `must` — без этого MVP не
  MVP; `nice` — хорошо бы, но переносимо. <!-- зачем: must-список = обязательные задачи, nice уходит в беклог, а не в стройку -->
  → Из документа (спека §3–§8). Всё `must`, потому что условие владельца — повторить артефакт:
  пространства (вкладка доски) и переключатель; один живой спринт на пространство; три входа набора
  состава; старт; сверка (три отметки + комментарий); «к переносу» с причиной; атомарная приёмка с
  автоматическим следующим черновиком и переносом; упоминание удалённых задач; ответственный и сроки
  инициативы; поле «Ссылки» в `TaskModal` над описанием; вид «Список» + «Канбан» переключателем;
  «Все инициативы»; шкала ритуала; KPI; аналитика (7 таблиц); журнал пространства; выгрузка итогов;
  фильтр по исполнителю и поиск; права «любой участник», удаление — админ; демо-пространство по
  содержанию артефакта (имена выдуманные). `nice` — нет.
- `[owner]` **Что нельзя ломать?** <!-- обязателен, если по секции 0 продукт уже работает: перечислить сценарии, которые обязаны продолжать работать как раньше. Зачем: это будущие регрессионные тесты, а не пожелание -->
  → Черновик из документа (спека §9, условие владельца «проверь максимально что он не ломает нашу
  текущую систему»), **ждёт подтверждения владельца**:
  1. доска проектов (`SprintBoard.tsx`): вкладки, секции, подпроекты, DnD, быстрое добавление;
  2. экран «Задачи»: списки, фильтры статусов, линзы «Мои/Команда», регулярные задачи, пинги;
  3. карточка задачи `TaskModal` на всех экранах (новое поле пустое по умолчанию);
  4. спринтовый канбан #267 (вид «Канбан» сохраняет поведение) и отчёт `SprintReport`;
  5. приватность задач и проектов (`is_private`, тумблер-глаз, наследование вниз);
  6. бот и MCP-инструменты задач (`get_tasks`, `update_task`, `get_task_history` и др.);
  7. удаление задач всеми путями (API, бот, MCP, удаление встречи ботом);
  8. старый веб в ночь раскатки (в промежутке между функциями и вебом).
- `[owner]` **Готовый контракт, который нельзя менять,** если он есть по секции 0:
  чужой API, формат обмена, схема данных. <!-- зачем: контракт превращается в проверяемое ограничение блока, а не в предмет обсуждения на брейншторме -->
  → Таблицы и роуты Swarm меняются только аддитивно; `GET /sprint-cycles` без параметра отдаёт всё,
  как раньше — из документа.
- `[owner]` **Аккаунты.** Нужны ли регистрация и вход, или продукт работает
  анонимно? <!-- зачем: самая дорогая развилка первой версии — тянет хранение, безопасность и восстановление доступа -->
  → Существующие (Telegram-логин, web-JWT), новых нет — из документа.
- `[owner]` **Права.** Если роли разные — кто что может и чего не может?
  <!-- зачем: без этого проверка прав размазывается по коду вместо одного места -->
  → Любой участник: создать, переименовать, сдвинуть, начать и принять спринт, сверка, перенос; админ —
  удалить спринт («давай пока оставим, потом ещё поменяем») — из документа.
- `[owner]` **Что продукт хранит** и есть ли среди этого персональные данные?
  <!-- зачем: определяет модель данных и требования к защите; персональные данные меняют требования к логам и бэкапам -->
  → Задачи, проекты, состав спринтов, журнал; имена и telegram id участников (уже есть в продукте).
  Новое: ссылки задач, ответственный инициативы (telegram id). Приватные задачи в спринты, отчёты и
  журнал не попадают — из документа.
- `[research]` **Есть ли готовые решения**, закрывающие большую часть этого?
  <!-- зачем: не писать своё там, где есть проверенное — это отдельная фаза исследования, но владелец мог уже что-то присмотреть -->
  → Спринтовая модель уже сверена с Plane (`Cycle`/`CycleIssue`, `transfer_cycle_issues`) при #267
  (`docs/superpowers/specs/2026-09-08-sprints-design.md`). В исследование: портфельные виды
  (инициативы → задачи) и ритуал сверки у аналогов.

## D. Референсы

<!-- Применимость: пропускается целиком, если по секции 0 есть готовый дизайн — тогда следуем макетам. Если аналогов владелец не знает, вопросы уходят в исследование, но секция остаётся. -->

- `[owner]` **«Хочу как тут»** — ссылки или примеры, и что именно в них
  нравится. <!-- зачем: «нравится» без «что именно» не переводится в решения по интерфейсу -->
  → Артефакт: шапка с KPI, вкладки, направление → инициатива с полоской прогресса, строка задачи с
  метками, сверка по ответственным, таблицы аналитики, журнал по дням — из документа.
- `[owner]` **Если есть аналог для повторения:** повторить поведение как есть
  или взять только идею? <!-- зачем: «как есть» — это проверяемое требование, «по мотивам» — свобода решений; смешивать нельзя -->
  → Повторить поведение как есть, кроме отменённого владельцем (спека §4) — из документа.
- `[owner]` **«Так точно не хочу»** — антипримеры. <!-- зачем: запреты экономят больше кругов правок, чем пожелания -->
  → Нативные выпадающие списки браузера: владелец назвал их «всратым контекстным меню», в Swarm для
  этого общий `ui/select` (память проекта, 09.09.2026).
- `[owner]` **Есть ли готовый бренд:** логотип, палитра, шрифты — или это тоже
  на систему? <!-- зачем: либо соблюдаем существующее, либо выбираем визуальное направление сами -->
  → Пропущен: готовый дизайн — дизайн-система веба Swarm.
- `[research]` **Конкуренты и их устройство.** <!-- зачем: уходит в исследование; здесь только то, что владелец знает сам -->
  → В исследование (вместе с C.«готовые решения»).

## E. Инфраструктура и доступы

<!-- Применимость: всегда; каждая подсекция — по нужности. Правило: доступ не считается существующим, пока смоук-команда не выполнена и не дала успех. Незаполненный или ❌ доступ = задача в беклог «выяснить доступы до раскатки» + работа идёт на тестовой площадке из профиля. -->

Поля в каждой подсекции: **есть?** (да / нет / не знаю) · **что именно**
(имя репозитория, хоста, базы) · **имя переменной** (не значение!) · **где
значение** (`.env`, менеджер секретов, «нет») · **смоук-команда** (чем
проверяется доступность) · **результат** (✅ / ❌ / не проверялось).

**Смоук-команда обязана падать, когда ресурса нет.** Это не формальность:
команда, которая при отсутствии ресурса всё равно возвращает успех, превращает
проверку в самообман. Классический пример — `dig +short домен`: на несуществующем
домене он молча отдаёт пустой вывод и код успеха, то есть «проверка» проходит
всегда. Проверять надо то, что реально нужно продукту: не «отвечает ли DNS», а
«отвечает ли адрес» (`curl -fsS <адрес>/healthz`); не «есть ли клиент базы», а
«отвечает ли база» (`psql "$DATABASE_URL" -c 'select 1'`). Если у команды пустой
вывод считается нормой — дополни её проверкой этого вывода, а не полагайся на код
возврата.

Если ресурс продукту **не нужен** — «есть?» = `нет`, в «что именно» одной фразой
почему не нужен, в остальных полях `—`. Если ресурс **нужен, но доступа нет** —
«есть?» = `нет` или `не знаю`, поля заполняются тем, что уже известно (имя
переменной, планируемая смоук-команда), результат `❌`, и в блоке «Осталось
выяснить» в конце анкеты появляется строка про этот ресурс. Разница между «не
нужен» и «нужен, но нет» — единственное, что здесь важно не перепутать: первое
закрывает вопрос, второе создаёт задачу.

### E1. GitHub

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| да | `GarroV/Swarm-brain` (**публичный**, намеренно), ветка `feat/initiatives-board` | — | keyring `gh` | `gh auth status` | ✅ 18.09.2026 (scopes repo, workflow) |

<!-- `[owner]` Куда складывать код: личный аккаунт или организация? Есть ли уже репозиторий, в который надо коммитить? Смоук: проверка авторизации в gh CLI. -->

### E2. Целевая площадка

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| да | веб — Cloudflare Pages, проект `swarm-brain` (собирает `main` сам) | — | дашборд Cloudflare | `curl -fsS -o /dev/null -w '%{http_code}' https://swarm-brain.pages.dev` | ✅ 200, 18.09.2026 |
| да | функции и БД — прод-проект Supabase Swarm; раскатка функций — workflow «Раскатка edge-функций (руками)», ночная — `deploy-window.yml` | секреты CI | GitHub Actions secrets | `gh workflow list --all` (оба active) | ✅ 18.09.2026 |
| — | **что живёт на площадке:** боевой продукт с живыми пользователями — это и есть наш продукт, чужого на ней нет; ограничение — раскатка только ночью 23:00–05:59 по Белграду по явному «да» владельца, PR с меткой `deploy-window` (CLAUDE.md п.5). Раскатка — не часть стройки FURCA | — | — | — | — |

<!-- `[owner]` Где продукт должен жить в итоге: свой сервер, облачный хостинг, ничего пока нет? Если продукт уже где-то работает — там же и остаётся. Смоук: вход на хост / вызов CLI хостинга. Если пусто — работаем на тестовой площадке из профиля, раскатка на целевую становится отдельной задачей.

`[research]` **Что уже живёт на этой площадке?** Выяснить смоуком, а не спрашивать: какие сервисы подняты, какие порты заняты, есть ли общая база и чьи в ней таблицы, публикуется ли уже что-то наружу с этого адреса. Площадка почти никогда не пустая, и чужое на ней — не фон, а ограничение: занятый порт, общий корень публичного адреса, база с чужими таблицами. На живом прогоне анкета выяснила «где продукт должен жить» и остановилась; чужой self-hosted стек на четырёх портах и чужие 18 таблиц в его базе всплыли позже, когда решения уже были приняты. Записать в таблицу отдельной строкой: что занято, кем, и что из этого нельзя трогать. -->

### E3. База данных

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| да | **локальный контур** `supabase start` (Postgres :54322, API :54321) — единственная база стройки | — | генерирует `supabase start` | `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc 'select 1'` + наличие `tasks`/`projects`/`sprints`/`sprint_cycles`/`sprint_items` | ✅ `1`, 5 из 5 таблиц, API 200 — 18.09.2026 |
| да | прод-база Swarm — **не для стройки** (живые пользователи); только чтение при сверке раскатки | — | MCP `supabase-swarm` | `select 1` через MCP | не проверялось в этой фазе: стройке не нужна |

<!-- `[owner]` Есть ли уже база или её нужно поднимать? Если есть — это боевая база с живыми данными или тестовая? Смоук: подключение и простейший запрос. ВАЖНО: наличие клиента (`psql`, `docker`, драйвер в зависимостях) ответом НЕ является — нужен отклик самого сервиса. `docker --version` печатает версию клиента и работает даже когда демон не запущен; `psql --version` — тем более. Отвечает база или нет, показывает только запрос к ней. -->

### E4. Домены

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| нет | не нужен: веб уже живёт на `swarm-brain.pages.dev` | — | — | — | — |

<!-- `[owner]` Есть ли домен, куда указывать? Смоук: проверка DNS. Нет домена — не блокер для MVP. -->

### E5. Сторонние API

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| нет | не нужны: фича не обращается к внешним сервисам | — | — | — | — |

<!-- `[owner]` Какие внешние сервисы нужны продукту и есть ли к ним ключи? Смоук: один настоящий вызов на минимальной операции. Строка на каждый сервис. -->

### E6. Платежи

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| нет | продукт не берёт денег | — | — | — | — |

<!-- `[owner]` Продукт берёт деньги? Если да — какой провайдер и есть ли тестовый режим. Смоук: только в тестовом режиме, никогда на живых платежах. -->

### E7. Аналитика

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| нет | не нужна: аналитика фичи — это её собственные таблицы по спринтам | — | — | — | — |

<!-- `[owner]` Нужно ли считать посещения и события, и есть ли уже счётчик? Смоук: доставка тестового события. -->

### E8. Существующие данные

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| да | артефакт: 5 направлений, 16 инициатив, 208 задач — эталон поведения; **оригинал только читаем**, заливка — этап 5 вне стройки | — | `~/Documents/workbench/private/swarm/initiatives-board-artifact.html` (84 КБ) | `grep -o '"id":"t' <путь> \| wc -l` → ждём 208 | ✅ 208, 18.09.2026 |

<!-- Заполняется, если по секции 0 есть данные. `[owner]` Где лежит источник, сколько там записей, в каком формате, и можно ли трогать оригинал или работаем только с копией? Смоук: чтение источника и подсчёт записей — до любой попытки переноса. Правило: оригинал по умолчанию только читаем; перенос идёт в новое место, чтобы откат был возможен.

`[owner]` **Лежит ли источник внутри репозитория продукта?** Спросить прямо и проверить самому (`git ls-files` по пути источника): файл в рабочей папке и файл под контролем версий — разные вещи, а выглядят одинаково. Если да — решить **до первого коммита**, что с ним: вынести за пределы репозитория, добавить в `.gitignore`, или оставить осознанно. Задним числом это лечится вычищением истории, то есть дорого и с потерями. Особенно важно, когда в источнике персональные данные: на живом прогоне таблица была помечена как содержащая их, но вопрос «а где она лежит» всплыл отдельным разговором уже после решений. Сплошной `git add` заберёт такой файл без единого вопроса. -->

→ Источник **вне репозитория** (`git ls-files` по имени — 0 совпадений): в нём внутренние задачи и имена
коллег, а репозиторий публичный. Оригинал владельца лежит неотслеживаемым в корне основной папки — по
глобальному правилу ему место в `~/Documents/workbench/private/`.

### E9. Адрес запуска на тестовой площадке

| есть? | что именно | имя переменной | где значение | смоук-команда | результат |
|---|---|---|---|---|---|
| да | Supabase local :54321 / :54322; веб `next dev` на :3107 (не :3000 — популярный порт) | `NEXT_PUBLIC_API_URL` | `miniapp/.env.local` (не в git) | `lsof -nP -iTCP:3107 -sTCP:LISTEN` — пусто | ✅ свободен, 18.09.2026 |

<!-- `[owner]` По какому адресу и порту продукт будет подниматься на проверочной площадке? Вопрос кажется мелким, но без него каждый агент выбирает порт наугад, а занятый чужим процессом порт выглядит как сломанный продукт. Смоук: порт свободен (`lsof -nP -iTCP:<порт> -sTCP:LISTEN` не возвращает строк) — и его надо выполнить, потому что на машине разработчика популярные порты обычно уже заняты другими проектами. Для продукта без сетевой поверхности — «не нужен». -->

## F. Ограничения

<!-- Применимость: всегда, но допустимо «ограничений нет». -->

- `[owner]` **Срок.** Есть ли дата или событие, к которому продукт нужен?
  <!-- зачем: срок меняет глубину первой версии, а не скорость работы -->
  → **Спросить владельца** (пробел: в документах срока нет).
- `[owner]` **Стек.** Есть ли предпочтения или прямые запреты по технологиям?
  <!-- зачем: запрет, узнанный после стройки, стоит переписывания блока. Если продукт уже существует — его стек и есть ответ -->
  → Стек продукта: Deno Edge Functions, Supabase Postgres, Next.js + React в `miniapp/`, общие модули
  `_shared/tasks/` — из документа. Файлы ≤ 800 строк; `swarm-api/index.ts` уже 2443 строки, поэтому
  новые роуты — только отдельными модулями (issue #265).
- `[owner]` **Деньги.** Сколько допустимо тратить на внешние сервисы в месяц?
  <!-- зачем: определяет выбор между платным сервисом и своей реализацией -->
  → Новых платных сервисов нет. Actions: репозиторий публичный, прогоны бесплатны; тяжёлых workflow не
  добавлять без расчёта.
- `[owner]` **Правовое и приватность.** Есть ли требования к тому, где и как
  хранятся данные? <!-- зачем: смена региона хранения после стройки — это переезд, а не настройка -->
  → Приватные задачи не попадают в спринты, отчёты и журнал; **репозиторий публичный — никаких
  внутренних данных и имён коллег в git**, демо — выдуманные имена. Хранение — там же, где весь
  Swarm (прод-проект Supabase).

## Покрытие

<!-- Заполняется в конце: карта полноты, чтобы полнота была видна, а не подразумевалась. Секция со «пусто» при статусе «нужна» — повод вернуться, а не идти дальше. -->

| Секция | Покрытие | Что ушло в исследование | Что ушло в дефолт |
|---|---|---|---|
| 0. Стартовая точка | полно | — | — |
| A. Продукт | полно (критерий MVP ждёт подтверждения) | — | — |
| B. Поверхности | полно | — | телефон/компьютер → компьютер первичен |
| C. Функционал | полно («что нельзя ломать» ждёт подтверждения) | готовые решения: портфельные виды, ритуал сверки | — |
| D. Референсы | полно (бренд пропущен: дизайн-система Swarm) | конкуренты | — |
| E. Инфраструктура и доступы | полно | — | — |
| F. Ограничения | частично (срок) | — | — |

<!-- Ниже — то, что осталось неопределённым после анкеты и должно попасть в вопросы владельцу или в беклог, а не растворяться. -->

**Осталось выяснить:**
- срок: есть ли дата, к которой «Качество» должно переехать в Swarm;
- подтверждение списка «что нельзя ломать» (C) и критерия успеха MVP (A);
- слияние файлов состояния FURCA в `main`, когда в одном репозитории три проекта FURCA (GarroV/furca#140).
