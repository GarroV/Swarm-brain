# Прод 2.0: настоящая система в новом визуале, на копии прода, на MUSPELHEIM

**Дата:** 24.09.2026 · **Статус:** пересмотрено в тот же день — см. «Пересмотр» внизу

## Слова владельца (дословно)
> новые вводные: команда дала добро на выгрузку. поэтому давай стенд делай непубличным, и загружай все по максимуму
> из прода. сделай по сути "прод 2.0" чтобы я проверил что вс еработает. и потом загрузим в прод. можно так?

На вопрос «прототип на данных или настоящий веб на копии базы»:
> что я хочу: увидеть полноценную нашу рабочую систему в новом визуале. протестировать . и если все ок - заливать ко
> всем пользователям и отключать старую

На вопрос «где держать»:
> реши сам как лучше сделать. я бы сделал локально но на муспельхейме

## Что это значит
- «Прод 2.0» — не статический прототип, а **настоящий веб Swarm в новом визуале** (`docs/redesign/stand/` — эталон
  вида и поведения), работающий против **копии прод-базы**: сохранение, доступы, приватность — по-настоящему.
- Размещение — **MUSPELHEIM, без публикации наружу** (доступ из локальной сети / по ssh-туннелю). По сути это
  возвращение погашенного 06.08.2026 staging-контура, но с новым интерфейсом.
- После проверки владельцем — раскатка ко всем (по правилам окна) и **отключение старого интерфейса**.
- Данные прода **не попадают в git**: репозиторий публичный. Дамп и выгрузки — `~/Documents/workbench/private/swarm/`
  и на сервере вне дерева репозитория. Согласие команды на выгрузку — получено (слова выше).

## Что ещё открыто
- Исходящие действия копии (бот в Telegram, письма, рекордер, cron) должны быть **выключены или направлены в тест**,
  иначе копия будет слать людям уведомления из прошлого. Проверить до первого запуска.
- Порядок переноса экранов и объём (все экраны стенда или сначала ядро) — план работ.
- Публичная копия стенда на GitHub Pages и данные владельца в ветке `feat/stand-visual` — убрать после переезда.

## Пересмотр 24.09.2026 (тот же день) — копии нет, витрина на живой базе
Слова владельца (дословно):
> почему мы не можем настроить витрину и привязать к нашей системе?
> у нс же все хранится на БД, зачем чт-то куда-то перегружать?!

Что это значит:
- **Копию прода не делаем, MUSPELHEIM не нужен.** Staging снова погашен (`docker compose down`), выгрузка не снималась.
- Новый визуал строится **в самом вебе** (`miniapp/`) в ветке. Cloudflare Pages сам собирает превью ветки, а прокси
  `/api` (`miniapp/functions/api`) у превью тот же, что у прода → **та же база, те же данные, вход по логину.**
- Все действия на витрине настоящие: это прод-данные, коллеги видят изменения, уведомления уходят как обычно.
- Раскатка ко всем = вливание ветки в `main` (по правилам окна), старый интерфейс уходит тем же движением.
- Проверить до старта: работает ли вход (Telegram / Google) на адресе превью.
