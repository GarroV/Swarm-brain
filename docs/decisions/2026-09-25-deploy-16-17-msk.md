# Раскатка 25.09.2026 — днём, 16:00–17:00 МСК, с объявлением

**Дата:** 2026-09-25

## Решение владельца (дословно)

> «повесь плашку, но сделай теперь что с 16 до 17 мск. И в этот час планируем раскатку»

## Что это меняет

- Раскатка, запланированная на ночь 25→26.09 (`docs/redesign/NEXT.md` в ветке `feat/stand-visual`), переносится на **16:00–17:00 МСК 25.09** (13:00–14:00 UTC). Это разовое исключение из ночного окна ([2026-08-27-deploy-at-night.md](2026-08-27-deploy-at-night.md)), принятое владельцем явно.
- Команде заранее показывается плашка в вебе (`app_settings.deploy_notice`, свой текст RU/EN): «не работайте в сервисе в это время: изменения и пометки к встречам могут не сохраниться».
- В порядок вливания добавлен #522 сразу после #508.

## Что учесть при раскатке

- Миграция #508 (`co_owners`) — до функций: `meeting-claim` пишет в новую колонку при каждой новой записи встречи.
- Заморозка (#507) отклоняет изменения в `swarm-api` для всех, кроме владельца, — в том числе живые пометки из панели рекордера. Сама запись встречи идёт мимо `swarm-api` и не страдает.

## Отменено в тот же день

Владелец остановил раскатку до её начала: «стопори раскатку» (передано сессией, которая вела
раскатку). Плашка не вешалась. Новую дату назначит владелец; #522 в порядке остаётся сразу
после #508.

Попутно: ни у одной сессии нет пути записи в прод — MCP только читает, токен CLI нужен другим
проектам и не трогается, отдавать владельцу SQL для ручного запуска не принято. Для плашки и
заморозки заведена кнопка в Actions — PR #526 (работает после вливания в `main`).
