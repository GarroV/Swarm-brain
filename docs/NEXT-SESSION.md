# Откуда продолжать (переписано 04.09.2026 ~21:00)

> Прошлая редакция (03.09) протухла: она вела на PR #212 и build 28, а PR закрыт
> без мёржа и рекордер уехал до 32. Держим этот файл коротким и датированным —
> длинный «дневник» здесь всегда расходится с реальностью.

## Состояние прода на 04.09.2026

- Рекордер: `LATEST_BUILD = 32`, эндпоинт проверен живьём —
  `curl .../functions/v1/swarm-recorder-version` → `{"build":32,...}`.
- Влито и раскатано днём: ON AIR/REC в панели встреч (#235), присутствие в
  heartbeat, защита значка в меню-баре, два фикса очереди ночной раскатки
  (#240, #241).
- Метка `prod-deployed` стоит на `8e84dcb` и отстала от факта — накопитель
  показывает `swarm-recorder-version` как нераскатанную, хотя она в проде.
  Разбор и варианты — [#243](https://github.com/GarroV/Swarm-brain/issues/243).
  Само себя вылечит первой же ночной раскаткой (она двигает метку).

## Очередь ночной раскатки (метка `deploy-window`)

Порядок — по дате создания PR, автоматика берёт самый старый:

1. **#215** — кнопка «Копировать» у тезисов + шапка «Обработано с помощью AI» (#214)
2. **#227** — иконка фидбека: речевой пузырь вместо знака вопроса
3. **#237** — упреждение встречи 5 минут вместо 10

Все три `MERGEABLE/CLEAN`, чеки зелёные. ⚠️ Летом ночью срабатывают **два**
прогона (23:00 и 00:00), то есть уедут два PR за ночь, а не один —
[#244](https://github.com/GarroV/Swarm-brain/issues/244).

## Ждёт живого взгляда, метку не вешать

- **#245** — профиль сотрудника, коннекторы плитками (#236). Собирается,
  632 теста зелёные, но экран в браузере никто не открывал — ни в светлой, ни в
  тёмной теме. Посмотреть → потом метка.
- **#231** — docs-only (карта проекта + этот файл). Раскатки не требует по
  сути, но мёрж в `main` всё равно пересобирает веб, поэтому тоже в окно.

## Что делать дальше

1. Открыть экран профиля и решить судьбу #245.
2. `agent_version` врёт (#209) — клиент по-прежнему шлёт хардкод `"0.1.0"`:
   `UploadQueue.swift:232`, `AppDelegate.swift:1481,1568,1581`. Сервер пишет что
   дадут, правка чисто клиентская. План: одна точка истины из `CFBundleVersion`,
   `VERSION` 32 → 33, сборка, заливка в Storage, живой прогон, потом `LATEST_BUILD`.
3. Долги по самой раскатке: #243 (метка), #244 (два прогона), #246 (`DRY_RUN=1`
   делает `reset --hard` рабочего дерева — локально сухой прогон не гонять).

## Границы

- Раскатка — только по «да» владельца в том же разговоре и в окно **23:00–05:59**.
- Метка на PR — это и есть раскатка отложенно: вешаем только на то, что реально
  проверено.
