# Откуда продолжать (переписано 04.09.2026 ~21:15, после вечерней раскатки)

> Держим файл коротким и датированным: прошлая редакция (03.09) вела на закрытый
> PR #212 и build 28, когда рекордер уже уехал до 32.

## Состояние прода на вечер 04.09.2026

- `main` = `6711c1b`. Раскатано вечером пятницы вне окна, по «ебаш сейчас»
  владельца; сторож активности перед заливкой дал 0 (никого).
- Уехало: **#215** (кнопка «Копировать» у тезисов + шапка «Обработано с помощью
  AI»), **#227** (иконка фидбека — речевой пузырь), **#237** (упреждение
  встречи 5 минут вместо 10).
- Функции: `meeting-current` v15; `swarm-recorder-version` без изменений.
  Метка `prod-deployed` → `6711c1b`.
- Рекордер: `LATEST_BUILD = 32`, эндпоинт версии отдаёт `{"build":32,...}`.
- Проверено живьём: в бандле есть новая иконка и нет старой, есть строки
  «Обработано с помощью AI» и «Копировать»; в задеплоенной функции
  `LOOKAHEAD_MIN = 5`; эндпоинт даёт 401 без токена.
- **Не проверено человеком:** клик по «Копировать» и уведомление рекордера за
  5 минут до встречи — нужен живой человек и настоящая встреча.

## Что открыто

- **PR #245** — профиль сотрудника, коннекторы плитками ([#236](https://github.com/GarroV/Swarm-brain/issues/236)).
  Собирается, 632 теста зелёные, но экран в браузере никто не открывал.
  Метку `deploy-window` не вешать, пока не посмотрели в светлой и тёмной теме.
- **[#209](https://github.com/GarroV/Swarm-brain/issues/209) `agent_version` врёт** — клиент шлёт хардкод `"0.1.0"`:
  `UploadQueue.swift:232`, `AppDelegate.swift:1481,1568,1581`. Сервер пишет что
  дадут, правка чисто клиентская. План: одна точка истины из `CFBundleVersion`,
  `VERSION` 32 → 33, сборка, заливка в Storage, живой прогон, потом `LATEST_BUILD`.
- Долги самой раскатки, найдены 04.09:
  [#243](https://github.com/GarroV/Swarm-brain/issues/243) (ручной деплой функций не двигает метку `prod-deployed`),
  [#244](https://github.com/GarroV/Swarm-brain/issues/244) (летом ночная автоматика срабатывает дважды за ночь),
  [#246](https://github.com/GarroV/Swarm-brain/issues/246) (`DRY_RUN=1` делает `reset --hard` рабочего дерева — локально сухой прогон НЕ гонять).

## Границы

- Раскатка — только по явному «да» владельца в том же разговоре. Окно
  23:00–05:59; вне окна — осознанный обход с его слова, и тогда обязателен
  `scripts/deploy-activity.sh` перед заливкой.
- Метка `deploy-window` на PR = отложенная раскатка. Вешать только на то, что
  реально проверено.
