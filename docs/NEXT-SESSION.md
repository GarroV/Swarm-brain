# Откуда продолжать (переписано 04.09.2026 ~23:50, после вечерней раскатки)

> Держим файл коротким и датированным: редакция от 03.09 вела на закрытый PR #212
> и build 28, когда рекордер уже уехал до 32.

## Состояние прода на конец 04.09.2026

`main` = `2720a9b`, метка `prod-deployed` там же. Раскатано вечером пятницы вне
окна, по «ебаш сейчас» владельца; перед каждой заливкой `scripts/deploy-activity.sh`
давал 0 (записи нет, обработка пуста, людей нет).

Уехало пятью PR:

| PR | Что |
|---|---|
| #215 | «Копировать» у тезисов + шапка «Обработано с помощью AI» (#214) |
| #227 | иконка фидбека — речевой пузырь вместо знака вопроса |
| #237 | упреждение встречи 5 минут вместо 10 |
| #231 | доки: карта проекта, запись о раскатке |
| #245 | профиль: коннекторы плитками со статусом (#236) |

- Функции: `meeting-current` v15; `swarm-recorder-version` без изменений.
- Рекордер: `LATEST_BUILD = 32`, эндпоинт версии отдаёт `{"build":32,...}`.
- Логи вокруг раскатки: 122 × 200, один 401 (свой же смоук), 5xx ноль.

**Проверено на живом проде:** в бандле есть новая иконка и нет старой; есть
«Обработано с помощью AI» и «Копировать»; чанк `0otg2xycyb1z~.js` содержит
«ПОДКЛЮЧЕНИЯ» и плитки коннекторов; в задеплоенной `meeting-current`
`LOOKAHEAD_MIN = 5`; эндпоинт даёт 401 без токена.

**Проверено в браузере (dev-режим, моки):** экран профиля в светлой и тёмной
теме на 1440px — сетка 5 плиток, «1 из 5», раскрытие панели по клику,
переключение между плитками; своих ошибок в консоли нет.

**Не проверено человеком:** клик по «Копировать» у залогиненного пользователя и
уведомление рекордера за 5 минут — нужен живой человек и настоящая встреча.

## Что открыто

- **[#209](https://github.com/GarroV/Swarm-brain/issues/209) `agent_version` врёт** — клиент шлёт хардкод `"0.1.0"`:
  `UploadQueue.swift:232`, `AppDelegate.swift:1481,1568,1581`. Сервер пишет что
  дадут, правка чисто клиентская. План: одна точка истины из `CFBundleVersion`,
  `VERSION` 32 → 33, сборка, заливка в Storage, живой прогон, потом `LATEST_BUILD`.
- **Найдено 04.09 при живой проверке, оба уже в проде:**
  [#247](https://github.com/GarroV/Swarm-brain/issues/247) — два одинаковых колокольчика в мобильной шапке (`RoyHeader` рисует свой, а экраны кладут в `right` ещё один через `HeaderActions`);
  [#248](https://github.com/GarroV/Swarm-brain/issues/248) — кнопка внутри кнопки в панели «Встречи сегодня», hydration error на каждом рендере дашборда.
- **Долги самой раскатки:**
  [#243](https://github.com/GarroV/Swarm-brain/issues/243) (ручной деплой функций не двигает метку `prod-deployed`),
  [#244](https://github.com/GarroV/Swarm-brain/issues/244) (летом ночная автоматика срабатывает дважды за ночь),
  [#246](https://github.com/GarroV/Swarm-brain/issues/246) (`DRY_RUN=1` делает `reset --hard` рабочего дерева — локально сухой прогон НЕ гонять).

## Границы

- Раскатка — только по явному «да» владельца в том же разговоре. Окно
  23:00–05:59; вне окна — осознанный обход с его слова, и тогда обязателен
  `scripts/deploy-activity.sh` перед заливкой.
- Метка `deploy-window` на PR = отложенная раскатка. Вешать только на то, что
  реально проверено.
