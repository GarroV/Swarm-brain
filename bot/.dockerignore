# Контекст сборки — каталог bot/. Всё, что образу не нужно, сюда не едет: без этого
# в контекст уходят сотни мегабайт node_modules, и сборка ждёт их на каждом запуске.
node_modules
coverage
dist
*.log
