# Коммит, от которого началась стройка scriba. Относительно него gate считает,
# какие файлы сервера тронула стройка (D013): их проверяют формат и линт, чужой
# легаси — нет.
#
# Точка зафиксирована ФАЙЛОМ, а не вычисляется через `git merge-base origin/main HEAD`.
# Причина измерена 17.09.2026: merge-base зависит от того, что знает конкретная копия
# о main, и в свежем клоне он оказался другим — гейт насчитал 11 затронутых файлов
# вместо 5 и покраснел на легаси, которого стройка не касалась. Гейт, дающий разный
# ответ на одном и том же коде, ничего не проверяет.
ae74d5ca23b12a4465a0d54fffd26a2f799fd20a
